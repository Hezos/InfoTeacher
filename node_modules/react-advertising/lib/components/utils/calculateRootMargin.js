"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports["default"] = calculateRootMargin;

var _isMobileDevice = _interopRequireDefault(require("./isMobileDevice"));

function calculateRootMargin() {
  var _ref = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {},
      marginPercent = _ref.marginPercent,
      mobileScaling = _ref.mobileScaling;

  if (!marginPercent) {
    return undefined;
  }

  var finalMarginPercent = (0, _isMobileDevice["default"])() && mobileScaling !== undefined && mobileScaling !== -1 ? marginPercent * mobileScaling : marginPercent;
  return "".concat(finalMarginPercent / 100 * window.innerHeight, "px");
}