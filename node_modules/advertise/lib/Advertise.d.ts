import AdvertiseTemplate from "./AdvertiseTemplate";
import { Options } from "./type/conf";
import Event from "./util/Event";
export declare type plugin = {
    install: (advertise: typeof Advertise, adtemp: typeof AdvertiseTemplate) => void;
    config?: object;
};
export default class Advertise {
    private template;
    uid: string;
    event: Event;
    options: any;
    constructor(options: Options);
    render(): void;
    destroy(): void;
    static use(plugin: any): void;
}
