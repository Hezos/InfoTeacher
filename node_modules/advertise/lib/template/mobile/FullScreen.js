"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const AdvertiseTemplate_1 = require("../../AdvertiseTemplate");
const constance_1 = require("../../util/constance");
let style = `
<style type="text/css">
  .ad-full{
    position: fixed;
    left: 0;
    top: 0;
    height:100vh;
    width:100vw;
    z-index: ${constance_1.MAX_Z_INDEX};
  }
  .ad-img{
    height:100%;
    vertical-align:bottom;
    width: 100%;
  }
  .auto-close{
    position: absolute;
    right: 5vw;
    top: 4vh;
  }
</style>
`;
class FullScreen extends AdvertiseTemplate_1.default {
    constructor(options) {
        super(options);
        this.styleSheet = style;
        this.defaultAnimation = '';
        this.default();
        this.createStyleSheet();
    }
    // 默认配置
    default() {
        this.templateOptions.target = document.body;
    }
    createTemplateStr() {
        let templateStr = `
    <div class="ad-full">
      ${this.adDesc}
      ${this.autoClose}
        <a href="${this.templateOptions.params[0].href}">
          <img class="ad-img" src="${this.templateOptions.params[0].img}" referrerpolicy="no-referrer"/>
        </a>
    </div>
    `;
        this.autoCloseFn(`.auto-close[${this.templateOptions.uid}]`);
        this.bindAnimation(`.ad-full[${this.templateOptions.uid}]`);
        return templateStr;
    }
}
exports.default = FullScreen;
