import AdvertiseTemplate from "../../AdvertiseTemplate";
import { animationName, TemplateOptions } from "../../type/conf";
export default class FullScreen extends AdvertiseTemplate {
    protected styleSheet: string;
    protected defaultAnimation: animationName;
    constructor(options: TemplateOptions);
    default(): void;
    createTemplateStr(): string;
}
