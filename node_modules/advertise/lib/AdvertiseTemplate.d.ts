import { SwiperOptions } from 'swiper';
import { TemplateOptions, animationName } from "./type/conf";
import Advertise from "./Advertise";
import Event from './util/Event';
export default abstract class AdvertiseTemplate {
    templateOptions: TemplateOptions;
    protected styleSheet: string;
    protected abstract defaultAnimation: animationName;
    protected autoClose: string;
    protected adDesc: string;
    advertise: Advertise;
    event: Event;
    static registry(): void;
    protected constructor(options: TemplateOptions);
    createStyleSheet(styleStr?: string): void;
    getTemplateStr(): string;
    close(): void;
    bindClose(selector: string | Element): void;
    autoCloseFn(selector: string | Element): void;
    bindAnimation(selector: any): void;
    bindSwiper(selector: any, options?: SwiperOptions): void;
    linkHandle(): void;
    protected abstract createTemplateStr(): string;
}
